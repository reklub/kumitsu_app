<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manage Club - <%= club.name %></title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
    <style>
        body {
            background: linear-gradient(135deg, #1e3c72, #2a5298);
            color: white;
            font-family: 'Poppins', sans-serif;
        }

        .container {
            margin-top: 50px;
            background: white;
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.2);
            color: black;
        }

        .participant-list {
            list-style: none;
            padding: 0;
        }

        .participant-list li {
            background: #f8f9fa;
            padding: 10px;
            margin: 5px 0;
            border-radius: 10px;
            font-weight: bold;
        }

        .btn-custom {
            background: #ff7eb3;
            border: none;
            padding: 10px 20px;
            border-radius: 25px;
            font-size: 18px;
            font-weight: bold;
            transition: 0.3s ease-in-out;
        }

        .btn-custom:hover {
            background: #ff4f91;
            transform: scale(1.05);
        }

        .back-link {
            text-decoration: none;
            color: #2a5298;
            font-weight: bold;
        }

        .back-link:hover {
            color: #ff4f91;
        }
    </style>
</head>
<body>

    <div class="container">
        <h2><i class="fas fa-users"></i> Manage Club: <%= club.clubName %></h2>
        <p>City: <%= club.clubCity %>, Country: <%= club.clubCountry %></p>

        <h3 class="mt-4">🏅 Participants:</h3>
        <% if (club.participants.length > 0) { %>
            <ul class="participant-list">
                <% club.participants.forEach(participant => { %>
                    <li>
                        <strong><%= participant.firstName %> <%= participant.lastName %></strong> 
                        (🎂 <%= participant.dateOfBirth.toISOString().split('T')[0] %>, ⚖ <%= participant.weight %>kg, 🥋 <%= participant.beltRank ? participant.beltRank.rank : 'N/A' %>, 👤 <%= getGenderText(participant.gender) %>)
                        <a href="/tournaments/<%= club.tournament._id || club.tournament %>/clubs/<%= club._id %>/participants/<%= participant._id %>/edit" class="btn btn-info btn-sm">Edit participant</a> 
                        <form method="POST" action="/tournaments/<%= club.tournament._id || club.tournament %>/clubs/<%= club._id %>/participants/<%= participant._id %>?_method=DELETE" style="display: inline;" onsubmit="return confirm('Are you sure you want to delete this participant?');">
                            <button type="submit" class="btn btn-danger btn-sm">Delete participant</button>
                        </form>
                    </li>
                    
                    
                <% }); %>
            </ul>
        <% } else { %>
            <p>No participants added yet.</p>
        <% } %>

        <% if (typeof currentUser !== 'undefined' && currentUser) { %>
        <h3 class="mt-4" id="toggleParticipants" style="cursor: pointer; display: flex; align-items: center;">➕ Add Participant:</h3>
        <div id="participantForm" style="display: none; margin-top: 10px;">
            <form action="/tournaments/<%= club.tournament._id || club.tournament %>/clubs/<%= club._id %>/add-participant" method="POST">
                <div class="mb-2">
                    <input type="text" class="form-control" name="firstName" placeholder="First Name" required>
                </div>
                <div class="mb-2">
                    <input type="text" class="form-control" name="lastName" placeholder="Last Name" required>
                </div>
                <div class="mb-2">
                    <input type="date" class="form-control" name="dateOfBirth" required>
                </div>
                <div class="mb-2">
                    <input type="number" step="0.1" class="form-control" name="weight" placeholder="Weight (kg)" required>
                </div>
                <div class="mb-2">
                    <select class="form-control" name="beltRank" required>
                        <option value="">Select Belt</option>
                        <% beltRanks.forEach(belt => { %>
                            <option value="<%= belt.rank %>"><%= belt.description || belt.rank.charAt(0).toUpperCase() + belt.rank.slice(1) %></option>
                        <% }); %>
                    </select>
                </div>
                <div class="mb-2">
                    <select class="form-control" name="gender" required>
                        <option value="">Select Gender</option>
                        <option value="male">male</option>
                        <option value="female">female</option>
                    </select>
                </div>
                <button type="submit" class="btn btn-custom w-100">Add Participant</button>
            </form>
        </div>
        <% } else { %>
        <div class="alert alert-info mt-4">
            <p><strong>Zaloguj się, aby dodać uczestników do klubu.</strong></p>
            <a href="/login" class="btn btn-primary">Zaloguj się</a>
        </div>
        <% } %>
        
        <br>
        <a href="/tournaments/<%= club.tournament._id || club.tournament %>" class="back-link"><i class="fas fa-arrow-left"></i> Back to Tournament</a>
    </div>

</body>
<script>
    <% if (typeof currentUser !== 'undefined' && currentUser) { %>
    document.getElementById("toggleParticipants").addEventListener("click", function() {
        let form = document.getElementById("participantForm");
        if (form.style.display === "none") {
            form.style.display = "block";
            this.innerHTML = "&#9660; Add Participant"; // Change arrow down when open
        } else {
            form.style.display = "none";
            this.innerHTML = "&#9654; Add Participant"; // Change arrow right when closed
        }
    });
    <% } %>
</script>
</html>
