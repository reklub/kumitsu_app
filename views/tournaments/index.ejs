<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tournament List</title>
    
    <!-- Bootstrap CSS (from CDN) -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Custom CSS (for additional styling) -->
    <link href="/stylesheets/tournamentStyle.css" rel="stylesheet">
</head>
<body>

    <div class="container mt-5">
        <div class="text-center mb-4">
            <h1 class="display-4">Upcoming Tournaments</h1>
            <p class="lead">Browse through the upcoming tournaments and join the action!</p>
        </div>

        <!-- Tournament List -->
        <div class="row">
            <% tournaments.forEach(tournament => { %>
                <div class="col-md-4 mb-4">
                    <div class="card shadow-lg">
                        <div class="card-body">
                            <h5 class="card-title"><%= tournament.name %></h5>
                            <h6 class="card-subtitle mb-2 text-muted">
                                <%= tournament.startDate.toLocaleDateString() %> 
                            </h6>
                            <p class="card-text"><%= tournament.description %></p>
                            <p class="card-text"><strong>Location:</strong> <%= tournament.location %></p>
                            <a href="/tournaments/<%= tournament._id %>" class="btn btn-primary">View Tournament</a>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>

        <!-- Add Tournament Button -->
        <div class="text-center mt-4">
            <a href="/tournaments/create-tournament" class="btn btn-success btn-lg">Create New Tournament</a>
        </div>
    </div>

    <!-- Bootstrap JS (for interactions like modals, dropdowns, etc.) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/javascripts/tournamentScript.js"></script>
</body>
</html>
